import { useState } from 'react'

export const useModal = () =>{
   

  return {
    modalBilling,
    handleModalBilling,
    modalAddProd,
    handleModalAddProd,
    modalAddClient,
    handleModalAddClient
  }
}

